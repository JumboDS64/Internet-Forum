<!DOCTYPE html>
<html>
  <head>
    <title>A forum.</title>
    <style>
      html {
        background:#f0f8ff;
      }
      h1 {
        color:#202830;
        border:5px solid #c0c8d0;
        background: linear-gradient(#f0f8ff, #c0c8d0);
      }
    </style>
  </head>
  <body>
    <h1>A forum.</h1>
    <ul>
      <% Message.all.each do |item| %>
      <li><%= item.user.name %>: <%= item.content %></li>
      <% end %>
    </ul>
    <form action="/new_message">
      User:<br>
      <select name="user_id">
        <% @users.each do |item| %>
        <option value="<% item.id %>">
          <%= item.name %>
        </option>
        <% end %>
      </select>
      Content:<br>
      <input type="text" name="content">
      <br>
      <input type="image" src="images/submit.png" alt="Submit">
    </form>
  </body>
</html>